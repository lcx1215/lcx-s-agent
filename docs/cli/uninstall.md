---
summary: "CLI reference for `lcx uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `lcx uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
lcx uninstall
lcx uninstall --all --yes
lcx uninstall --dry-run
```
