---
summary: "CLI reference for `lcx clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `lcx clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `lcx clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `lcx clawbot qr` (same behavior as [`lcx qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `lcx clawbot qr` -> `lcx qr`
