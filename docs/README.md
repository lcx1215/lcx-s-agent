# Docs

Owns operator-facing concepts, runbooks, install guides, integration guides,
and public explanations for the current Agent system. Runtime lineage, vendor
names, and channel choices are implementation details: verify them from the
current profile and source instead of treating this index as authority.
